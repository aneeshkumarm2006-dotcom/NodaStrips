import { StubPage } from "@/components/StubPage";

export default function NotFound() {
  return (
    <StubPage
      index="404"
      title="Nothing here yet"
      note="This page has not been built. The main page is the first pass — everything else follows."
    />
  );
}

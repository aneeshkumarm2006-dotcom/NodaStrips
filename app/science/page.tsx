import { StubPage } from "@/components/StubPage";

export const metadata = { title: "Science — NODA" };

export default function Page() {
  return <StubPage index="Science" title="Science" note="The strip format and the evidence behind it, in the calm editorial register of Seed." />;
}

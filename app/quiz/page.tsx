import { StubPage } from "@/components/StubPage";

export const metadata = { title: "Take the quiz — NODA" };

export default function Page() {
  return <StubPage index="Quiz" title="Take the quiz" note="Entry to personalization. Question flow and formula logic to be scoped." />;
}

import { StubPage } from "@/components/StubPage";

export const metadata = { title: "Account — NODA" };

export default function Page() {
  return <StubPage index="Account" title="Account" note="Sign in and order history. Waiting on the commerce platform decision." />;
}

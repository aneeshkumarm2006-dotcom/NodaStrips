import { StubPage } from "@/components/StubPage";

export const metadata = { title: "Subscription — NODA" };

export default function Page() {
  return <StubPage index="Subscription" title="Subscription" note="Recurring delivery and first access. Pricing and cadence still to be decided." />;
}
